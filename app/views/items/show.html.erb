<p id="notice"><%= notice %></p>

<p>
    <div>
      <h2>Product name: <%= @item.name %></h2>
      <%= image_tag("#{@item.name}.jpg", alt: "Rails")%>

      <div id="comment-list">
        <%=  render 'list' %>
      </div>

    </div>
</p>
<p>
      <div id="comment-form">
        <%= render 'comments/form' %>
      </div>
      <%= link_to 'Back', items_path %>

</p>